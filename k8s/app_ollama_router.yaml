apiVersion: apps/v1
kind: Deployment
metadata:
  name: jervis-ollama-router
  namespace: jervis
spec:
  revisionHistoryLimit: 2
  replicas: 1
  selector:
    matchLabels:
      app: jervis-ollama-router
  template:
    metadata:
      labels:
        app: jervis-ollama-router
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: router
          image: registry.damek-soft.eu/jandamek/jervis-ollama-router:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 11430
          envFrom:
            - configMapRef:
                name: jervis-config
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          startupProbe:
            httpGet:
              path: /router/health
              port: 11430
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 30  # 10 + 30*10 = 310s max startup (model preload takes 2-3 min)
          livenessProbe:
            httpGet:
              path: /router/health
              port: 11430
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 6
          readinessProbe:
            httpGet:
              path: /router/health
              port: 11430
            periodSeconds: 10
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  name: jervis-ollama-router
  namespace: jervis
spec:
  selector:
    app: jervis-ollama-router
  ports:
    - protocol: TCP
      port: 11430
      targetPort: 11430
  type: ClusterIP
---
# Ingress for local access from GPU VM (whisper → router coordination).
# HTTP only — local domain, no TLS (Let's Encrypt won't sign .local).
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jervis-ollama-router-ingress
  namespace: jervis
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "60"
spec:
  rules:
    - host: jervis-router.lan.mazlusek.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jervis-ollama-router
                port:
                  number: 11430
    - host: jervis-router
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jervis-ollama-router
                port:
                  number: 11430
