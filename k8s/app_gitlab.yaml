apiVersion: apps/v1
kind: Deployment
metadata:
  name: jervis-gitlab
  namespace: jervis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jervis-gitlab
  template:
    metadata:
      labels:
        app: jervis-gitlab
    spec:
      imagePullSecrets:
        - name: registry-credentials
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: service-gitlab
          image: registry.damek-soft.eu/jandamek/jervis-service-gitlab:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8086
              name: rpc
          env:
            - name: PORT
              value: "8086"
            - name: HOST
              value: "0.0.0.0"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "768Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 8086
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 10
            failureThreshold: 6
          readinessProbe:
            httpGet:
              path: /
              port: 8086
            initialDelaySeconds: 20
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /
              port: 8086
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 12
---
apiVersion: v1
kind: Service
metadata:
  name: jervis-gitlab
  namespace: jervis
spec:
  selector:
    app: jervis-gitlab
  ports:
    - name: rpc
      protocol: TCP
      port: 8086
      targetPort: 8086
  type: ClusterIP
