apiVersion: apps/v1
kind: Deployment
metadata:
  name: jervis-coding-engine
  namespace: jervis
spec:
  revisionHistoryLimit: 2
  replicas: 1
  selector:
    matchLabels:
      app: jervis-coding-engine
  template:
    metadata:
      labels:
        app: jervis-coding-engine
    spec:
      containers:
        - name: coding-engine
          image: registry.damek-soft.eu/jandamek/jervis-coding-engine:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3200
          livenessProbe:
            httpGet:
              path: /
              port: 3200
            initialDelaySeconds: 45
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /
              port: 3200
            initialDelaySeconds: 30
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /
              port: 3200
            failureThreshold: 30
            periodSeconds: 10
          volumeMounts:
            - name: jervis-data
              mountPath: /opt/jervis/data
      volumes:
        - name: jervis-data
          persistentVolumeClaim:
            claimName: jervis-data-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: jervis-coding-engine
  namespace: jervis
spec:
  selector:
    app: jervis-coding-engine
  ports:
    - name: krpc
      port: 3200
      targetPort: 3200
