apiVersion: apps/v1
kind: Deployment
metadata:
  name: jervis-tika
  namespace: jervis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jervis-tika
  template:
    metadata:
      labels:
        app: jervis-tika
    spec:
      containers:
      - name: tika
        image: registry.damek-soft.eu/jandamek/jervis-tika:latest
        ports:
        - containerPort: 8080
        env:
        - name: SERVER_PORT
          value: "8080"
        - name: WORK_DATA
          value: "/opt/jervis/work"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: jervis-tika
  namespace: jervis
spec:
  selector:
    app: jervis-tika
  ports:
  - port: 8080
    targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jervis-joern
  namespace: jervis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jervis-joern
  template:
    metadata:
      labels:
        app: jervis-joern
    spec:
      containers:
      - name: joern
        image: registry.damek-soft.eu/jandamek/jervis-joern:latest
        ports:
        - containerPort: 8080
        env:
        - name: SERVER_PORT
          value: "8080"
        - name: WORK_DATA
          value: "/opt/jervis/work"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: jervis-joern
  namespace: jervis
spec:
  selector:
    app: jervis-joern
  ports:
  - port: 8080
    targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jervis-whisper
  namespace: jervis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jervis-whisper
  template:
    metadata:
      labels:
        app: jervis-whisper
    spec:
      containers:
      - name: whisper
        image: registry.damek-soft.eu/jandamek/jervis-whisper:latest
        ports:
        - containerPort: 8080
        env:
        - name: SERVER_PORT
          value: "8080"
        - name: WORK_DATA
          value: "/opt/jervis/work"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: jervis-whisper
  namespace: jervis
spec:
  selector:
    app: jervis-whisper
  ports:
  - port: 8080
    targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jervis-aider
  namespace: jervis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jervis-aider
  template:
    metadata:
      labels:
        app: jervis-aider
    spec:
      containers:
      - name: aider
        image: registry.damek-soft.eu/jandamek/jervis-aider:latest
        ports:
        - containerPort: 3100
        env:
        - name: SERVER_PORT
          value: "3100"
        - name: DATA_ROOT_DIR
          value: "/opt/jervis/data"
        livenessProbe:
          httpGet:
            path: /health
            port: 3100
          initialDelaySeconds: 45
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 3100
          initialDelaySeconds: 30
          periodSeconds: 10
        startupProbe:
          httpGet:
            path: /health
            port: 3100
          failureThreshold: 30
          periodSeconds: 10
        volumeMounts:
        - name: jervis-data
          mountPath: /opt/jervis/data
      volumes:
      - name: jervis-data
        persistentVolumeClaim:
          claimName: jervis-data-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: jervis-aider
  namespace: jervis
spec:
  selector:
    app: jervis-aider
  ports:
  - port: 3100
    targetPort: 3100
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jervis-coding-engine
  namespace: jervis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jervis-coding-engine
  template:
    metadata:
      labels:
        app: jervis-coding-engine
    spec:
      containers:
      - name: coding-engine
        image: registry.damek-soft.eu/jandamek/jervis-coding-engine:latest
        ports:
        - containerPort: 3200
        env:
        - name: SERVER_PORT
          value: "3200"
        - name: DATA_ROOT_DIR
          value: "/opt/jervis/data"
        livenessProbe:
          httpGet:
            path: /health
            port: 3200
          initialDelaySeconds: 45
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 3200
          initialDelaySeconds: 30
          periodSeconds: 10
        startupProbe:
          httpGet:
            path: /health
            port: 3200
          failureThreshold: 30
          periodSeconds: 10
        volumeMounts:
        - name: jervis-data
          mountPath: /opt/jervis/data
      volumes:
      - name: jervis-data
        persistentVolumeClaim:
          claimName: jervis-data-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: jervis-coding-engine
  namespace: jervis
spec:
  selector:
    app: jervis-coding-engine
  ports:
  - port: 3200
    targetPort: 3200
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jervis-atlassian
  namespace: jervis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jervis-atlassian
  template:
    metadata:
      labels:
        app: jervis-atlassian
    spec:
      containers:
      - name: atlassian
        image: registry.damek-soft.eu/jandamek/jervis-atlassian:latest
        ports:
        - containerPort: 8080
        env:
        - name: SERVER_PORT
          value: "8080"
        - name: WORK_DATA
          value: "/opt/jervis/work"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: jervis-atlassian
  namespace: jervis
spec:
  selector:
    app: jervis-atlassian
  ports:
  - port: 8080
    targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jervis-server
  namespace: jervis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jervis-server
  template:
    metadata:
      labels:
        app: jervis-server
    spec:
      containers:
      - name: server
        image: registry.damek-soft.eu/jandamek/jervis-server:latest
        ports:
        - containerPort: 5500
        envFrom:
        - configMapRef:
            name: jervis-server-config
        env:
        # Secrets (passwords, API keys)
        - name: MONGODB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: jervis-secrets
              key: MONGODB_PASSWORD
        - name: ARANGO_PASSWORD
          valueFrom:
            secretKeyRef:
              name: jervis-secrets
              key: ARANGO_PASSWORD
        - name: ANTHROPIC_API_KEY
          valueFrom:
            secretKeyRef:
              name: jervis-secrets
              key: ANTHROPIC_API_KEY
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: jervis-secrets
              key: OPENAI_API_KEY
        - name: GOOGLE_API_KEY
          valueFrom:
            secretKeyRef:
              name: jervis-secrets
              key: GOOGLE_API_KEY
        - name: ATLASSIAN_TOKEN
          valueFrom:
            secretKeyRef:
              name: jervis-secrets
              key: ATLASSIAN_TOKEN
        - name: JUNIE_API_KEY
          valueFrom:
            secretKeyRef:
              name: jervis-secrets
              key: JUNIE_API_KEY
        volumeMounts:
        - name: jervis-data
          mountPath: /opt/jervis/data
        livenessProbe:
          httpGet:
            path: /health
            port: 5500
          initialDelaySeconds: 45
          periodSeconds: 15
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 5500
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: 5500
          failureThreshold: 30
          periodSeconds: 10
      volumes:
      - name: jervis-data
        persistentVolumeClaim:
          claimName: jervis-data-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: jervis-server
  namespace: jervis
spec:
  selector:
    app: jervis-server
  ports:
  - name: http
    port: 5500
    targetPort: 5500
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jervis-junie
  namespace: jervis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jervis-junie
  template:
    metadata:
      labels:
        app: jervis-junie
    spec:
      containers:
      - name: junie
        image: registry.damek-soft.eu/jandamek/jervis-junie:latest
        ports:
        - containerPort: 3300
        env:
        - name: SERVER_PORT
          value: "3300"
        - name: DATA_ROOT_DIR
          value: "/opt/jervis/data"
        - name: JUNIE_API_KEY
          valueFrom:
            secretKeyRef:
              name: jervis-secrets
              key: JUNIE_API_KEY
        livenessProbe:
          httpGet:
            path: /health
            port: 3300
          initialDelaySeconds: 45
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 3300
          initialDelaySeconds: 30
          periodSeconds: 10
        startupProbe:
          httpGet:
            path: /health
            port: 3300
          failureThreshold: 30
          periodSeconds: 10
        volumeMounts:
        - name: jervis-data
          mountPath: /opt/jervis/data
      volumes:
      - name: jervis-data
        persistentVolumeClaim:
          claimName: jervis-data-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: jervis-junie
  namespace: jervis
spec:
  selector:
    app: jervis-junie
  ports:
  - port: 3300
    targetPort: 3300
