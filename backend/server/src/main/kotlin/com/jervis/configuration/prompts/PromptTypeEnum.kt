package com.jervis.configuration.prompts

/**
 * Prompt type enumeration.
 * Restored with EMBEDDING only (other types intentionally omitted for now).
 */
enum class PromptTypeEnum {
    EMBEDDING,
}
