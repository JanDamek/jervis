package com.jervis.domain.symbol

/**
 * Enum representing different kinds of symbols in code
 */
enum class SymbolKind {
    CLASS,
    INTERFACE,
    METHOD,
    FUNCTION,
    PROPERTY,
    FIELD,
    VARIABLE,
    CONSTRUCTOR,
    ENUM,
    ANNOTATION,
    PACKAGE,
    MODULE,
    NAMESPACE,
    UNKNOWN
}