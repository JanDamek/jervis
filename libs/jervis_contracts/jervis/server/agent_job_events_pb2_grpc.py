# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc
import warnings

from jervis.server import agent_job_events_pb2 as jervis_dot_server_dot_agent__job__events__pb2

GRPC_GENERATED_VERSION = '1.78.0'
GRPC_VERSION = grpc.__version__
_version_not_supported = False

try:
    from grpc._utilities import first_version_is_lower
    _version_not_supported = first_version_is_lower(GRPC_VERSION, GRPC_GENERATED_VERSION)
except ImportError:
    _version_not_supported = True

if _version_not_supported:
    raise RuntimeError(
        f'The grpc package installed is at version {GRPC_VERSION},'
        + ' but the generated code in jervis/server/agent_job_events_pb2_grpc.py depends on'
        + f' grpcio>={GRPC_GENERATED_VERSION}.'
        + f' Please upgrade your grpc module to grpcio>={GRPC_GENERATED_VERSION}'
        + f' or downgrade your generated code using grpcio-tools<={GRPC_VERSION}.'
    )


class AgentJobEventsServiceStub(object):
    """AgentJobEventsService — server-streaming push of AgentJobRecord state
    changes. Subscribers (typically the Python orchestrator's per-client
    session loops) receive one message per state transition emitted by
    `AgentJobDispatcher.saveAndEmit` / `AgentJobWatcher`. The orchestrator
    then injects `[agent-update] ...` system messages into the LLM session
    queue so the chat model sees state changes without polling.

    Pairs with NotificationRpcImpl.subscribeToEvents (kRPC, UI-side) —
    the gRPC bridge filters that broadcast for AgentJobStateChanged events
    only and re-emits them as proto messages.
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.Subscribe = channel.unary_stream(
                '/jervis.server.AgentJobEventsService/Subscribe',
                request_serializer=jervis_dot_server_dot_agent__job__events__pb2.AgentJobEventsSubscribeRequest.SerializeToString,
                response_deserializer=jervis_dot_server_dot_agent__job__events__pb2.AgentJobStateChangedEvent.FromString,
                _registered_method=True)


class AgentJobEventsServiceServicer(object):
    """AgentJobEventsService — server-streaming push of AgentJobRecord state
    changes. Subscribers (typically the Python orchestrator's per-client
    session loops) receive one message per state transition emitted by
    `AgentJobDispatcher.saveAndEmit` / `AgentJobWatcher`. The orchestrator
    then injects `[agent-update] ...` system messages into the LLM session
    queue so the chat model sees state changes without polling.

    Pairs with NotificationRpcImpl.subscribeToEvents (kRPC, UI-side) —
    the gRPC bridge filters that broadcast for AgentJobStateChanged events
    only and re-emits them as proto messages.
    """

    def Subscribe(self, request, context):
        """Long-lived server stream. The server sends one message per emit,
        and never completes the call from its side. Clients reconnect with
        exponential backoff on transport drops (no polling fallback).
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_AgentJobEventsServiceServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'Subscribe': grpc.unary_stream_rpc_method_handler(
                    servicer.Subscribe,
                    request_deserializer=jervis_dot_server_dot_agent__job__events__pb2.AgentJobEventsSubscribeRequest.FromString,
                    response_serializer=jervis_dot_server_dot_agent__job__events__pb2.AgentJobStateChangedEvent.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'jervis.server.AgentJobEventsService', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('jervis.server.AgentJobEventsService', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class AgentJobEventsService(object):
    """AgentJobEventsService — server-streaming push of AgentJobRecord state
    changes. Subscribers (typically the Python orchestrator's per-client
    session loops) receive one message per state transition emitted by
    `AgentJobDispatcher.saveAndEmit` / `AgentJobWatcher`. The orchestrator
    then injects `[agent-update] ...` system messages into the LLM session
    queue so the chat model sees state changes without polling.

    Pairs with NotificationRpcImpl.subscribeToEvents (kRPC, UI-side) —
    the gRPC bridge filters that broadcast for AgentJobStateChanged events
    only and re-emits them as proto messages.
    """

    @staticmethod
    def Subscribe(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_stream(
            request,
            target,
            '/jervis.server.AgentJobEventsService/Subscribe',
            jervis_dot_server_dot_agent__job__events__pb2.AgentJobEventsSubscribeRequest.SerializeToString,
            jervis_dot_server_dot_agent__job__events__pb2.AgentJobStateChangedEvent.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
