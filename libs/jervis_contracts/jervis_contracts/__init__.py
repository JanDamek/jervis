"""Jervis inter-service contract stubs.

Generated Protobuf + gRPC stubs under `_generated/` are populated by
`buf generate` (run via the top-level `make proto-generate`). Hand-written
helpers live next to this file (e.g. `interceptors/`).
"""

__all__ = ["interceptors"]
